
package proyecpoo;

/**
 *
 * @author valen
 */
public class ProyecPOO {
    
    
 /**
     * El método 'main' es el punto de entrada de la aplicación.
     * @param args
     */
    public static void main(String[] args) {
        
        // 1. (Opcional) Inicializar la conexión a la base de datos
        // dao.ConexionMongoDB.getDatabase();
        
        // 2. Crear una instancia del formulario de Login
        LoginFrame login = new LoginFrame();
        
        // 3. Hacer el formulario visible
        login.setVisible(true); 
        
        // El resto de la aplicación se ejecuta dentro del 'Event Dispatch Thread' (EDT) 
        // gestionado por Swing.
    }
}
